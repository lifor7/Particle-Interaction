# 阶段 2 开发规划

## 总则

在阶段 1 已完成的 Three.js 粒子代码（`粒子辉光.html`）基础上迭代开发，**不改动原有基础渲染架构**。

## 需求 1：色彩分层优化

**目标**：严格按圈层配色，打破阶段 1 的随机双色分配方式。

| 圈层 | 半径范围 | 配色 | 说明 |
|------|----------|------|------|
| 内核聚集区 | 0 ~ 40% R | 玫红 `#e85090`、粉紫 `#d070d0` | 中心最密集区域 |
| 环形过渡带 | 40% ~ 75% R | 青蓝 `#50c8d8`、浅蓝 `#78b8f0` | 中间过渡层 |
| 外围弥散区 | 75% ~ 100% R | 淡蓝 `#a0d8f8`、纯白 `#f0f4ff` | 最外层稀疏区域 |

**实现思路**：
- 在 `positions` 计算结束后，根据每个粒子的实际半径 `r` 判定所属圈层
- 从该圈层调色板中随机选色（加上 HSL 微调）
- 圈层边界做柔和过渡（随机扰动半径阈值 ±5%），避免出现生硬分界

## 需求 2：左侧悬浮控制面板

**目标**：放置配色一键切换按钮 + 粒子大小全局调节滑块，简约不遮挡视野。

**面板内容**：
- **配色方案按钮组**：
  - "默认"：当前分层配色（内核粉紫 → 外围淡蓝）
  - "星云"：紫红系渐变（适合模拟星云效果）
  - "深海"：蓝白色系（冷色调）
  - "日落"：橙金到紫罗兰渐变
- **粒子大小滑块**：`<input type="range">`，调节 `PointsMaterial.size`（范围 0.05 ~ 0.5）

**样式要求**：
- 固定于视口左侧，`position: fixed; left: 20px; top: 50%; transform: translateY(-50%)`
- 半透明毛玻璃效果（`backdrop-filter: blur(12px)`，`rgba(10,14,24,0.75)` 背景）
- 圆角细边框，字体 13px，颜色浅灰
- 宽度约 200px，不遮挡中心粒子主视觉区

## 需求 3：粒子布朗运动

**目标**：全部粒子带有微弱的随机布朗运动，保持自然流动感。

**实现思路**：
- 在动画循环中更新 `geometry.attributes.position`，每帧给每个粒子位置加上微小随机偏移
- 偏移幅度建议：`±0.003` / 帧（60fps 下约 0.18 单位/秒）
- 每粒子保有独立的"游走相位"，用正弦函数平滑方向变化
- 偏移后粒子仍被约束在原始半径 ±10% 范围内，防止扩散过度
- 使用 `geometry.attributes.position.needsUpdate = true` 每帧标记更新

## 需求 4：辉光分层

**目标**：内核区域辉光范围更大、亮度更高，外围粒子辉光偏弱，杜绝过曝糊成一团。

**实现思路**：
- 内核粒子 `size` 设为 0.28（较大），外围粒子 `size` 设为 0.22（较小）
- 内核粒子颜色亮度整体提高 15%
- 降低 UnrealBloomPass 的 `radius`（如从 0.5 降至 0.35），使辉光更收敛
- 降低 `strength` 基础值（如从 1.4 降至 1.15），配合强度分层
- 在 `vertexColors` 中使用**颜色的亮度通道**控制辉光：
  - Bloom 阈值 `threshold: 0.1` 表示 RGB 亮度 > 0.1 的像素才发光
  - 内核粒子颜色 RGB 更亮 → 自然获得更强辉光
  - 外围粒子颜色 RGB 更暗 → 辉光偏弱
- 整体不做过曝，保持层次清晰

## 需求 5：约束条件

- 保持单 HTML 文件结构
- 代码注释延续清晰风格
- 暂不接入摄像头手势识别相关逻辑
